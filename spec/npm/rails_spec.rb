require 'spec_helper'

describe Npm::Rails do
  it 'has a version number' do
    expect(Npm::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
